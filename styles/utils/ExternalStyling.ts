import { StyleSheet } from "react-native";

export const externalStyling = StyleSheet.create({
    textStyle: {
        color: "white",
        fontSize: 30,
        backgroundColor: "teal",
        margin: 20,
        padding: 20,
    },
})
